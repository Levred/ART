// AVEC_MODULE START DELETE
------------- ex_module1.py --------------------

__init__(self,nom,attribut1,attribut2) -> None 

initialise l'objet

// SOMME START DELETE  
Objet.sum(<(MODIF)>modif<(/MODIF)>) -> int/float

Revoie la somme des deux attributs d'un objet<(MODIF)> avec en y ajoutant un modificatieur<(/MODIF)>.
// AFFICHER_SOMME START DELETE
Objet.afficher_somme(<(MODIF)>modif<(/MODIF)>) -> None

Affiche le rédultat de la somme des deux attributs d'un objet<(MODIF)> en y ajoutant un modificatieur<(/MODIF)>.
// AFFICHER_SOMME STOP DELETE
// SOMME STOP DELETE

// AVEC_MODULE STOP DELETE
// not AVEC_MODULE START DELETE
----------- ex.main.py -------------------------

creer_objet(nom,attribut1,attrbut2) -> list[str,float,float]

initialise l'objet

// SOMME START DELETE  
sum(pbjet,<(MODIF)>modif<(/MODIF)>) -> int/float

Revoie la somme des deux attributs d'un objet<(MODIF)> avec en y ajoutant un modificatieur<(/MODIF)>.
// AFFICHER_SOMME START DELETE
Objet.afficher_somme(objet,<(MODIF)>modif<(/MODIF)>) -> None

Affiche le rédultat de la somme des deux attributs d'un objet<(MODIF)> en y ajoutant un modificatieur<(/MODIF)>.
// AFFICHER_SOMME STOP DELETE
// SOMME STOP DELETE
// not AVEC_MODULE STOP DELETE